<div dir="rtl">

# تطبيق تبادل مستلزمات المساجد — مديرية أوقاف دمشق

نظام متكامل يعمل فعلياً: **سيرفر خلفي + قاعدة بيانات + واجهة ويب** تعمل على الهاتف والحاسوب.
مبني بـ Node.js المدمج فقط — **بدون أي مكتبات خارجية** ولا حاجة لـ `npm install`.

## التشغيل (الطريقة السهلة — ويندوز)

1. ثبّت **Node.js** من https://nodejs.org (تثبيت عادي، مرة واحدة).
2. انقر نقراً مزدوجاً على ملف **`تشغيل-التطبيق.bat`**.
3. سيفتح المتصفح تلقائياً على `http://localhost:3000`.

## التشغيل (يدوياً — أي نظام)

```bash
cd awqaf_app
node server.js
```
ثم افتح المتصفح على `http://localhost:3000`.

## الحسابات التجريبية

يمكن الدخول بالبريد الإلكتروني أو رقم الجوال:

| الدور | البريد الإلكتروني | رقم الجوال |
|------|------------------|------------|
| قيّم مسجد (جامع الإيمان) | `ahmad@awqaf-damas.gov.sy` | `0911000001` |
| قيّم مسجد (الجامع الأموي) | `mahmoud@awqaf-damas.gov.sy` | `0911000002` |
| موظف الوزارة (الموافقات) | `ministry@awqaf-damas.gov.sy` | `0922000000` |
| المدير العام (الإشراف) | `manager@awqaf-damas.gov.sy` | `0933000000` |

> رمز التحقق (OTP) يظهر تلقائياً على الشاشة في النسخة التجريبية. عند الربط ببوابة بريد أو رسائل SMS حقيقية يُرسَل إلى المستخدم ويُحذف ظهوره.

## التدفّق الكامل

1. **قيّم المسجد** يصوّر غرضاً فائضاً ويرفعه (زر +) → يصبح *متاحاً*.
2. **قيّم مسجد آخر** يطلبه → يصبح *بانتظار موافقة الوزارة*.
3. **موظف الوزارة** يوافق أو يرفض **كل عملية منفردة**.
4. بعد الموافقة، **المسجد المصدر** يؤكّد التسليم → *تم التسليم*.
5. **المدير العام** يتابع الإحصاءات وسجل كل العمليات.

## البنية

```
awqaf_app/
  server.js            ← السيرفر الخلفي + الـ API (Node.js، بلا مكتبات)
  data.json            ← قاعدة البيانات (تُنشأ تلقائياً عند أول تشغيل)
  uploads/             ← صور الأغراض المرفوعة
  public/
    index.html         ← واجهة التطبيق (ويب + PWA للهاتف)
    manifest.json      ← إعداد التثبيت على الهاتف
    icon.svg           ← أيقونة التطبيق
  تشغيل-التطبيق.bat     ← مشغّل بنقرة واحدة (ويندوز)
```

## واجهة الـ API (مرجع سريع)

- `POST /api/auth/request-otp` · `POST /api/auth/verify-otp`
- `GET /api/items?scope=browse|mine|myrequests` · `POST /api/items` · `DELETE /api/items/:id`
- `POST /api/items/:id/request` · `POST /api/items/:id/deliver`
- `GET /api/requests?status=pending` · `POST /api/requests/:id/approve` · `POST /api/requests/:id/reject`
- `GET /api/stats/overview`

## الخطوات نحو الإنتاج

هذا النظام جاهز للتشغيل والتجربة محلياً. لنشره للعموم:

1. **الاستضافة**: رفع المجلد على خادم/استضافة (أو سيرفر الوزارة) وتشغيل `node server.js` خلف نطاق مع شهادة SSL.
2. **قاعدة البيانات**: لأحمال أكبر يُستبدل ملف `data.json` بقاعدة بيانات (MySQL/PostgreSQL) — البنية معدّة لذلك.
3. **رسائل SMS**: ربط بوابة SMS لإرسال رمز الدخول فعلياً.
4. **تطبيقا أندرويد وآيفون**: تغليف الواجهة كتطبيق Flutter/PWA ونشره على المتجرين (راجع وثيقة المواصفات التقنية).
5. **الشعار الرسمي**: استبدال الأيقونة المؤقتة بالشعار الجديد للمديرية.

</div>
